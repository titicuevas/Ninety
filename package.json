{
  "name": "ninety",
  "version": "0.2.0",
  "private": true,
  "description": "Tu diario futbolero — guarda y revive todos los partidos que has visto",
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "test": "npm run test --prefix backend",
    "check:secrets": "node scripts/check-secrets.mjs",
    "vapid:set-railway": "node scripts/set-railway-vapid.mjs",
    "seed:test-user": "node scripts/seed-test-user.mjs",
    "seed:demo": "npm run generate:test-photos --prefix backend && npm run seed:demo --prefix backend",
    "doctor": "react-doctor frontend --verbose",
    "smoke:v1": "npm run smoke:v1 --prefix backend",
    "demo:flow": "npm run demo:flow --prefix backend",
    "test:v1": "npm run test --prefix backend && npm run demo:flow --prefix backend",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:smoke": "playwright test --project=smoke-public --project=chromium --grep @smoke",
    "test:e2e:public": "playwright test --project=smoke-public --project=a11y",
    "test:e2e:critical": "playwright test --project=chromium --project=mobile --project=tablet --project=desktop-shell --grep @critical",
    "test:e2e:a11y": "playwright test --project=a11y",
    "test:e2e:mobile": "playwright test --project=mobile --project=tablet",
    "test:qa": "npm test && npm run test --prefix frontend && npm run test:e2e:public",
    "doctor:install": "react-doctor install -y"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "concurrently": "^9.1.2",
    "dotenv": "^17.4.2",
    "react-doctor": "^0.7.1"
  },
  "overrides": {
    "@oxc-parser/binding-linux-x64-gnu": "0.135.0"
  },
  "optionalDependencies": {
    "@oxc-parser/binding-linux-x64-gnu": "0.135.0"
  }
}
